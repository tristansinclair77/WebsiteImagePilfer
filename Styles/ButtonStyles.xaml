<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Base Button Style -->
    <Style x:Key="BaseButtonStyle" TargetType="Button">
        <Setter Property="Height" Value="{StaticResource ButtonHeightStandard}"/>
   <Setter Property="Padding" Value="{StaticResource PaddingSmall}"/>
        <Setter Property="BorderThickness" Value="1"/>
     <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
     <Setter Property="Cursor" Value="Hand"/>
      <Style.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
       <Setter Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
       <Setter Property="BorderBrush" Value="{StaticResource BorderHoverBrush}"/>
    </Trigger>
  <Trigger Property="IsPressed" Value="True">
     <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
      <Setter Property="Foreground" Value="White"/>
     </Trigger>
            <Trigger Property="IsEnabled" Value="False">
  <Setter Property="Opacity" Value="0.5"/>
         <Setter Property="Cursor" Value="Arrow"/>
     </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Standard Button (80px wide) -->
    <Style x:Key="StandardButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Width" Value="{StaticResource ButtonWidthStandard}"/>
    </Style>
    
    <!-- Narrow Button (60px) -->
    <Style x:Key="NarrowButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
  <Setter Property="Width" Value="{StaticResource ButtonWidthNarrow}"/>
    </Style>
    
    <!-- Wide Button (100px) -->
    <Style x:Key="WideButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
        <Setter Property="Width" Value="{StaticResource ButtonWidthWide}"/>
    </Style>
    
    <!-- Extra Wide Button (130px) -->
    <Style x:Key="ExtraWideButton" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
   <Setter Property="Width" Value="{StaticResource ButtonWidthExtraWide}"/>
    </Style>
    
    <!-- Small Button (Height=28) -->
    <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource StandardButton}">
        <Setter Property="Height" Value="{StaticResource ButtonHeightSmall}"/>
        <Setter Property="Padding" Value="6,3,6,3"/>
        <Setter Property="MinHeight" Value="28"/>
    </Style>
    
    <!-- Small Wide Button -->
    <Style x:Key="SmallWideButton" TargetType="Button" BasedOn="{StaticResource WideButton}">
        <Setter Property="Height" Value="{StaticResource ButtonHeightSmall}"/>
        <Setter Property="Padding" Value="6,3,6,3"/>
      <Setter Property="MinHeight" Value="28"/>
    </Style>
    
    <!-- Small Narrow Button -->
 <Style x:Key="SmallNarrowButton" TargetType="Button" BasedOn="{StaticResource NarrowButton}">
        <Setter Property="Height" Value="{StaticResource ButtonHeightSmall}"/>
        <Setter Property="Padding" Value="6,3,6,3"/>
        <Setter Property="MinHeight" Value="28"/>
    </Style>
    
    <!-- Tiny Button (Height=26) -->
    <Style x:Key="TinyButton" TargetType="Button" BasedOn="{StaticResource StandardButton}">
        <Setter Property="Height" Value="{StaticResource ButtonHeightTiny}"/>
        <Setter Property="Padding" Value="5,3,5,3"/>
        <Setter Property="MinHeight" Value="26"/>
    </Style>
    
    <!-- Primary Action Button (Blue) -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource StandardButton}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
    <Setter Property="Foreground" Value="White"/>
 <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
           <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
       </Trigger>
      <Trigger Property="IsPressed" Value="True">
          <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
        </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Primary Wide Button -->
    <Style x:Key="PrimaryWideButton" TargetType="Button" BasedOn="{StaticResource WideButton}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
<Style.Triggers>
       <Trigger Property="IsMouseOver" Value="True">
   <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
      </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
         </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Primary Extra Wide Button -->
    <Style x:Key="PrimaryExtraWideButton" TargetType="Button" BasedOn="{StaticResource ExtraWideButton}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
     <Setter Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
       </Trigger>
            <Trigger Property="IsPressed" Value="True">
       <Setter Property="Background" Value="{StaticResource PrimaryPressedBrush}"/>
       </Trigger>
        </Style.Triggers>
    </Style>
  
</ResourceDictionary>
